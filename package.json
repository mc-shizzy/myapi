{
  "name": "moviebox-stream-proxy",
  "version": "1.0.0",
  "description": "MovieBox stream proxy for Northflank (apii) — API JSON is on Cloudflare (apiv1)",
  "main": "proxy-server.js",
  "scripts": {
    "start": "node proxy-server.js",
    "start:api": "node index.js",
    "dev": "node proxy-server.js",
    "deploy:worker": "wrangler deploy"
  },
  "dependencies": {
    "express": "^4.21.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
