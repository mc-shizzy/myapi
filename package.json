{
  "name": "moviebox-api",
  "version": "1.0.0",
  "description": "MovieBox API — Node.js server (converted from Cloudflare Worker)",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "start:api": "node index.js",
    "start:proxy": "node proxy-server.js",
    "dev": "node index.js",
    "deploy:worker": "wrangler deploy"
  },
  "dependencies": {
    "express": "^4.21.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
